<?xml version="1.0"?>
<!--
  Generated by stoatworks-unraid/scripts/gen-templates.mjs.
  Edit fleet.json / unraid.json and regenerate — direct edits are overwritten.
-->
<Container version="2">
  <Name>openflash</Name>
  <Repository>ghcr.io/stoatworks-labs/openflash:latest</Repository>
  <Registry>https://ghcr.io/stoatworks-labs/openflash</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/stoatworks-labs/openflash/issues</Support>
  <Project>https://github.com/stoatworks-labs/openflash</Project>
  <Overview>Installs LineageOS and other alternative Android systems onto a phone from a browser, over WebUSB. Detects the handset and generates the procedure for that exact device.

Note: WebUSB needs a secure context. Served over plain http at a LAN address there is no navigator.usb at all, so the container cannot reach a device — open it at http://localhost:PORT on the machine holding the USB cable, or put a TLS terminator in front of it. This fails silently rather than loudly: the page loads and simply offers no device. Without WebUSB the page is still a correct, ordered, device-specific adb and fastboot command list, which is most of its value.</Overview>
  <Category>Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:80]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/templates/openflash.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>
  <Config Name="WebUI Port" Target="80" Default="8536" Mode="tcp" Description="Host port for the web interface." Type="Port" Display="always" Required="true" Mask="false">8536</Config>
</Container>
