<?xml version="1.0"?>
<!--
  Generated by stoatworks-unraid/scripts/gen-templates.mjs.
  Edit fleet.json / unraid.json and regenerate — direct edits are overwritten.
-->
<Container version="2">
  <Name>aquilon-vpu-map</Name>
  <Repository>ghcr.io/stoatworks-labs/aquilon-vpu-map:latest</Repository>
  <Registry>https://ghcr.io/stoatworks-labs/aquilon-vpu-map</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support></Support>
  <Project></Project>
  <Overview>Shows how an Analog Way LivePremier has allocated its VPU mixers across screens, layers and slices — what each screen is spending, what is left, and what a pending configuration would change. Reads only; it never writes to the switcher.

Note: Reads only — it issues AWJ `get` and never writes to the switcher, so it is safe to leave running against a live rig. Bridge networking is deliberate: it discovers nothing and makes one outbound connection to the address you type, so it needs no host networking. hasOwnDocker because the repo's Dockerfile is hand-written and its CI runs the container and checks /api/health — a generated one would build for a `npm run build` and a dist/ this app does not have.</Overview>
  <Category>MediaApp:Video Tools:</Category>
  <WebUI>http://[IP]:[PORT:8531]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/templates/aquilon-vpu-map.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>
  <Config Name="WebUI Port" Target="8531" Default="8531" Mode="tcp" Description="Host port for the web interface." Type="Port" Display="always" Required="true" Mask="false">8531</Config>
</Container>
