<?xml version="1.0"?>
<!--
  Generated by stoatworks-unraid/scripts/gen-templates.mjs.
  Edit fleet.json / unraid.json and regenerate — direct edits are overwritten.
-->
<Container version="2">
  <Name>birddog-play-flasher</Name>
  <Repository>ghcr.io/stoatworks-labs/birddog-play-flasher:latest</Repository>
  <Registry>https://ghcr.io/stoatworks-labs/birddog-play-flasher</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/stoatworks-labs/birddog-play-flasher/issues</Support>
  <Project>https://github.com/stoatworks-labs/birddog-play-flasher</Project>
  <Overview>Flashes a BirdDog PLAY decoder in recovery mode over WebUSB, and can inject a firmware package on the way so the unit installs it on first boot.

Note: WebUSB needs a secure context. Served over plain http at a LAN address there is no navigator.usb at all, so the container cannot reach a device — open it at http://localhost:PORT on the machine holding the USB cable, or put a TLS terminator in front of it. This fails silently rather than loudly: the page loads and simply offers no device. This app is nothing but WebUSB, so without it there is no fallback at all. public/ is committed already built, hence no build stage; there is no _headers file in this repo, so the image carries the default header set.</Overview>
  <Category>MediaApp:Video Tools:</Category>
  <WebUI>http://[IP]:[PORT:80]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/templates/birddog-play-flasher.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>
  <Config Name="WebUI Port" Target="80" Default="8539" Mode="tcp" Description="Host port for the web interface." Type="Port" Display="always" Required="true" Mask="false">8539</Config>
</Container>
