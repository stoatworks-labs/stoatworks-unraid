<?xml version="1.0"?>
<!--
  Generated by stoatworks-unraid/scripts/gen-templates.mjs.
  Edit fleet.json / unraid.json and regenerate — direct edits are overwritten.
-->
<Container version="2">
  <Name>srt-router</Name>
  <Repository>ghcr.io/stoatworks-labs/srt-router:latest</Repository>
  <Registry>https://github.com/orgs/stoatworks-labs/packages</Registry>
  <Network>host</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/stoatworks-labs/srt-router/issues</Support>
  <Project>https://github.com/stoatworks-labs/srt-router</Project>
  <Overview>Crosspoint router for SRT video streams, with a web control UI.

Note: Built with default features. NDI is included but loads its runtime with dlopen at run time, so the image links no NDI SDK and ships no NDI runtime — the NDI transport is simply unavailable inside the container unless you mount a runtime yourself. The `omt` feature is left off deliberately: omt-io still links its SDK at build time, and that SDK is not redistributable.

This container uses host networking because it discovers devices over mDNS/broadcast, which Docker's bridge network does not reliably forward. In bridge mode discovery finds nothing rather than reporting an error.</Overview>
  <Category>MediaApp:Video Tools:</Category>
  <WebUI>http://[IP]:8080/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/templates/srt-router.xml</TemplateURL>
  <Icon></Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>
  <Config Name="Config" Target="/app/config" Default="/mnt/user/appdata/srt-router/config" Mode="rw" Description="Configuration files." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/srt-router/config</Config>
  <Config Name="Data" Target="/app/state" Default="/mnt/user/appdata/srt-router/data" Mode="rw" Description="Persistent application data." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/srt-router/data</Config>
</Container>
