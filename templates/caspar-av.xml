<?xml version="1.0"?>
<!--
  Generated by stoatworks-unraid/scripts/gen-templates.mjs.
  Edit fleet.json / unraid.json and regenerate — direct edits are overwritten.
-->
<Container version="2">
  <Name>caspar-av</Name>
  <Repository>ghcr.io/stoatworks-labs/caspar-av:latest</Repository>
  <Registry>https://github.com/orgs/stoatworks-labs/packages</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/stoatworks-labs/caspar-av/issues</Support>
  <Project>https://github.com/stoatworks-labs/caspar-av</Project>
  <Overview>CasparCG show control server — cue lists, AMCP bridging and OSC control.

Note: Two things a Rust-only Dockerfile gets wrong here. `web/` is NOT tracked in git — console/vite.config.ts builds into ../web with emptyOutDir — so the image needs a Node stage before the Rust one or it ships a server whose ServeDir points at nothing and 404s every page. And `--web` defaults to the relative path `web`, which resolves against the working directory, so the runtime stage passes an absolute /app/web rather than relying on WORKDIR.</Overview>
  <Category>MediaApp:Video Tools:</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/templates/caspar-av.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/stoatworks-labs/stoatworks-unraid/main/icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>
  <Config Name="WebUI Port" Target="8080" Default="8092" Mode="tcp" Description="Host port for the web interface." Type="Port" Display="always" Required="true" Mask="false">8092</Config>
  <Config Name="Config" Target="/app/config" Default="/mnt/user/appdata/caspar-av/config" Mode="rw" Description="Configuration files." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/caspar-av/config</Config>
  <Config Name="Data" Target="/app/data" Default="/mnt/user/appdata/caspar-av/data" Mode="rw" Description="Persistent application data." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/caspar-av/data</Config>
</Container>
